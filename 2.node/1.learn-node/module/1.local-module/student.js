const student = (name, roll, subject) => {
  console.log(
    `My name is ${name}. Roll Number ${roll} & my subject ${subject}`
  );
};

//exports.student = student;
//module.exports = student;
export default student;
